import { newQuestion } from 'message-board/helpers/new-question';
import { module, test } from 'qunit';

module('Unit | Helper | new question');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = newQuestion([42]);
  assert.ok(result);
});
