{{#if addAnswer}}
<form>
  <div class="form-group">
    <label for="author">Your Name:</label>
    {{input value=author id="author"}}
  </div>
  <div class="form-group">
    <label for="content">Your Answer:</label>
    {{input value=content id="content"}}
  </div>
<button class="btn btn-default btn-lg" {{action 'saveAnswer'}}>Submit Answer</button>
</form>
{{else}}
<button class="btn btn-default btn-lg" {{action 'answerFormShow'}}>Reply to this Question</button>
{{/if}}
